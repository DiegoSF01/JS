// ? = SIM
// : = NÃO

let isMember = true;

let shipping = isMember ? 2 : 10;

console.log(`Frete: ${shipping}`);

let age = 18;

let isAdult = age >= 18 && age < 60 ? 'Sim' : 'Não';

console.log(isAdult);